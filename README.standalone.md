# Standalone export

This tree was generated by `scripts/export-standalone-npm-package.sh simulator-core` from the DeliveryPlus monorepo.

```bash
git init
git branch -M main
git add .
git commit -m "Sync simulator-core from DeliveryPlus"
git remote add origin git@github.com:SignalSafeSoftware/simulator-core.git
git push -u origin main
```

See `RELEASING.md` for npm publish steps.
