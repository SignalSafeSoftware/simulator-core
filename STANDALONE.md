# Standalone repository

This package is maintained in its own GitHub repository. Install dependencies from the committed Yarn lockfile; release builds must not depend on a sibling source checkout. Historical monorepo export scripts are not part of the supported release path. See [RELEASING.md](RELEASING.md) for tag-triggered GitHub publication.
